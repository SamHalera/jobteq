{% extends "base.html.twig" %}
{% block body %}
	<div class="container">

		<a href="{{path('app_job_offer_admin_index')}}" class="btn btn-info my-4">back</a>
		<h1>EDIT
			<span class="text-primary">{{jobOffer.title}}</span>
		</h1>

		<img src="{{vich_uploader_asset(jobOffer, 'thumbnailFile')}}" class="w-25 img-thumbnail" alt=" ">

		{{form_start(form)}}


		{{form_end(form)}}

	</div>
{% endblock %}
